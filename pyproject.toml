[project]
name = "claude-git-guard"
version = "0.1.0"
description = "Claude Code PreToolUse hook that blocks state-changing git subcommands"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
